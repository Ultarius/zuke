name: library_catalog
description: Specs-first pure Dart example that showcases the Zuke analyzer plugin.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.10.0 <4.0.0'
resolution: workspace

dependencies:
  zuke_annotations: ^0.4.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.26.0
  zuke: ^0.4.0
  zuke_analyzer: ^0.1.0
  zuke_cli: ^0.5.0
  zuke_runner: ^0.4.0
