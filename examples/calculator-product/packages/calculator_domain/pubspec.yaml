name: calculator_domain
description: Calculator domain logic with specification annotations.
version: 1.0.0
publish_to: none
environment:
  sdk: '>=3.10.0 <4.0.0'
resolution: workspace
dependencies:
  calculator_contracts: ^1.0.0
  zuke_annotations: ^0.1.0
dev_dependencies:
  test: ^1.31.1
