# Changelog

## 0.1.0

- Initial public preview release.
- Adds the `zuke_missing_test` rule (`ZUKE-MISSING-TEST`) when an implemented
  requirement has no workspace `@VerifiesRequirement` in the current index.
